#include<bits/stdc++.h>
using namespace std;

class newton{
public:
    double x,y,z;
    void set_func(){

        }
    float u_cal(float u, int n)
    float temp = u;
    for (int i = 1; i < n; i++)
        temp = temp * (u - i);
    return temp;
}
    };
int main(){
    int a[101];
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    }
